
# TODO(Codex): Implement AcquisitionManager as follows
# - The AcquisitionManager controls the peripherals to take images
# - The peripheral it takes are LEDManager, filterhweel, dmd, and camera
# - It also takes a filemanager that will be used in case the images are saved
# - The main function it implements for now is take_frame, which takes a frame config, actuates peripherals, and returns the image
# - Suggest any other functions it should implement while making the plan